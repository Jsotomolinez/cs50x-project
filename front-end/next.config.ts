import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["m.media-amazon.com"], // Agrega aquí los dominios permitidos
  },
  /* otras opciones de configuración aquí */
};

export default nextConfig;
